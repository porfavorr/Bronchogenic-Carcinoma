#!/bin/bash
echo "Starting the Cancer Detection Streamlit App..."
# This script assumes it's run from the project root directory.
# If src/app.py is not found, Streamlit will show an error.
# Ensure your current working directory is BTech_Cancer_Detection_Project/ when running this.
streamlit run src/app.py